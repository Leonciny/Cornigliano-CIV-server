import express from "express";

/**
 * ROUTER OF AWARDS
 */

const router = express.Router()
//index
router.get("/",( req, res)=>{
})
//store
router.post("/",(req, res)=>{
})

module.exports = router
